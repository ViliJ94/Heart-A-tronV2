# PC Companion Application Requirements
# Install with: pip install -r requirements.txt

# MQTT:
# This project uses a Mosquitto broker, and the PC companion uses the Mosquitto CLI
# tools (mosquitto_pub / mosquitto_sub) instead of a Python MQTT library.

# Tkinter is included with Python standard library
# But on some Linux systems, you may need to install separately:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora: sudo dnf install python3-tkinter
# macOS: Usually pre-installed with Python

# Optional logging enhancement
colorama==0.4.6
